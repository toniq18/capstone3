import { checkForName } from "../src/client/js/nameChecker"


describe("Testing to see if checkForName is defined", () => {
    test("Test the checkForName function", () => {
        expect(checkForName).toBeDefined();
    })});